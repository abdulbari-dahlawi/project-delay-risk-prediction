# Results

Publication-facing `FINAL_*.csv` and `FINAL_*.json` outputs are generated by the notebooks.

For the journal release, preserve the exact result files used to populate the manuscript tables and figures. Consider placing the final lightweight CSV/JSON result set in a tagged GitHub release and the full archive in Zenodo.
