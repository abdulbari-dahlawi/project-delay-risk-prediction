# Generated Outputs

This directory is populated when the notebooks run.

Expected subdirectories: `data/`, `models/`, `results/`, and `plots/`.

Large generated data/model artifacts are excluded from Git by `.gitignore`.
