# Models

Fitted model binaries are generated by Notebook 02 and ignored by Git. Archive the exact publication models in Zenodo if required.

Expected artifacts include the selected model pipeline, model metadata, deep-learning assets, and probability-calibration artifacts.
