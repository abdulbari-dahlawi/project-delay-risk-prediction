# Data

The main 500,000-row dataset is generated by Notebook 01 and is intentionally not committed to Git.

`new_project_template.csv` provides the expected predictor schema for optional scenario prediction. Copy it to `new_project.csv` and populate one or more rows.

Do not place confidential or real organizational data in a public repository.
